__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/
.coverage
.DS_Store

# Local secrets / configuration (use .env.example as the template)
.env

# Datasets — keep large JSON files out of git; download per README.
data/*
!data/README.md

# Model weights — never commit these
models/
